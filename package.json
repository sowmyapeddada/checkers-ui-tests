{
  "name": "checkers-ui-tests",
  "version": "1.0.0",
  "description": "Playwright and Jest UI tests for Checkers game",
  "scripts": {
    "test": "playwright test",
    "test:dev": "ENV=dev playwright test",
    "test:prod": "ENV=prod playwright test"
  },
  "dependencies": {
    "@playwright/test": "^1.48.0",
    "dotenv": "^16.4.5",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
